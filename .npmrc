# Prevent malicious packages from executing lifecycle scripts (preinstall/postinstall/install)
# during npm install. This mitigates supply-chain attacks.
# If a package legitimately needs scripts (e.g. native bindings), install it explicitly:
#   npm install --ignore-scripts false <package>
ignore-scripts=true
